# CLAUDE.md — SpringZhihu TDD 实战教程

> 每次对话启动时自动加载。包含项目上下文、写作规范、操作约定。

---

## 项目身份

- **项目名称**：SpringZhihu TDD 实战教程（build-zhihu-with-springboot-and-tdd-manual）
- **项目定位**：纯文档仓库。通过从零构建仿知乎问答系统，教授 Spring Boot 测试驱动开发
- **目标读者**：会写业务代码但不会写测试的 Java/Spring 开发者
- **源码仓库**：[build-zhihu-with-springboot-and-tdd](https://github.com/qianhuihuiji/build-zhihu-with-springboot-and-tdd)（独立仓库，本文档不包含源码）
- **文档仓库**：[build-zhihu-with-springboot-and-tdd-manual](https://github.com/qianhuihuiji/build-zhihu-with-springboot-and-tdd-manual)（当前仓库）

### 技术栈

| 层级 | 技术 |
|------|------|
| 应用框架 | Spring Boot 3.x, Spring Security |
| ORM | MyBatis, PageHelper |
| 数据库 | MySQL 8.0 (Docker), Flyway 迁移 |
| 缓存/消息 | Redis, Kafka |
| 测试 | JUnit 5, Mockito, AssertJ, Testcontainers, Spring Test |

### 仓库结构

```
docs/
├── 1.基础信息/       # 序言、技术栈介绍、测试框架前置知识
├── 2.舞台布置/       # 环境准备、产品需求、用例分析
├── 3.开始测试/       # 项目脚手架、第一个测试、TDD 循环
├── 4.显示问题/       # 问题列表、详情、集成测试 vs 单元测试
├── 5.回答问题/       # 回答 CRUD、表单验证、最佳答案、权限
├── 6.赞成与反对/     # 投票功能
├── 7.添加问题/       # 创建问题、表单验证、@用户
├── 8.问题互动/       # 筛选、关注、推荐、举报、slug
├── 9.评论相关/       # 评论 CRUD、评论投票
└── 10.用户相关/      # 用户动态、活跃用户、未读消息
```

---

## 写作风格规范

### 顶层定位：实战优先

本教程是**实战手册**，不是技术综述。周志明《凤凰架构》的写法偏重"讲清楚技术全景和来龙去脉"，适合架构师建立认知框架。但本教程的目标是**让读者动手做出来**——快速建立肌肉记忆，获得可迁移的测试技能。

因此，写作遵循以下优先级：

```
可操作 > 可理解 > 可追溯
```

- **可操作**（最高优先级）：读者读完一节，必须能动手做一件事——运行一个命令、写一段代码、看到一个结果。如果一节读完没有"能做"的东西，这节需要重写。
- **可理解**：在操作之后/之中，解释为什么这样做。用痛点驱动，不用历史驱动。
- **可追溯**（最低优先级）：历史背景、技术演进等内容仅在必要时以最简形式出现（一句话或一个链接），不展开。

### 从周志明那里借什么，不借什么

| 借 | 不借 |
|---|------|
| 痛点驱动 Why（"没有 Testcontainers 你会踩什么坑"） | 历史全景 Why（"测试框架从 JUnit 1 到 5 四十年的演进"） |
| 半口语化对话感（"你看"、"说白了"） | 架构综述式的广度铺陈 |
| 用统一框架串联知识点（测试分层金字塔） | 深入算法/协议级别的技术细节 |
| 核心隐喻贯穿（"测试是安全网"） | 大段引用论文/理论（ARIES、冯·诺依曼等） |
| 代码+文字严格对应 | — |

### 核心写作原则

1. **先跑起来，再理解**：每节的开头不是"我们先来了解 X 的概念"，而是"来，跑一下这个命令/写一下这段代码"。看到结果之后，再解释刚才发生了什么。

2. **痛点驱动 Why，不是历史驱动 Why**：
   - ✅ 好的 Why："传统用 H2 替代 MySQL 跑测试，方言差异导致过'测试全绿、上线炸了'。Testcontainers 用 Docker 启动真 MySQL，测完销毁——测试环境和生产环境终于说同一种语言。"
   - ❌ 坏的 Why："软件测试自 1957 年 Charles Baker 在《软件测试》一书中首次提出以来，经历了手工测试、录制回放、关键字驱动、数据驱动四个阶段……Testcontainers 诞生于容器化浪潮之中……"（读者已经关了页面）

3. **做中学**：教程的第一驱动力是"动手"。理论解释不超前于操作——读者手上已经有了一行能跑的代码，你再去解释它为什么能跑。

4. **半口语化，不装腔作势**：保持对话感（"你看"、"说白了"、"好戏还没开场"），但关键术语必须给出准确定义。技术书写出"人味儿"。

### 段落节奏

- 密集代码/技术段落后必须有小标题分隔或过渡句
- 每个大节末尾应有简短的"到这里我们完成了 X"作为锚点
- 章节末尾有小结（回顾本章学到了什么 + 引出下一章）

### 代码示例规范

- **严禁修改、删除、截断、缩写文档中的任何代码块。** 这些代码与配套源码仓库一一对应，读者会逐行跟着敲。即使一个 import、一行空行、一处注释都不能少。违反此规则会导致读者跟着敲出来的代码与源码仓库不一致、编译失败或行为异常。
- **严禁用 "// ... 省略 ..."、"// ... 略 ..."、"/* ... 同前 ... */" 等任何形式替代代码块中的实际代码。** 每个代码块必须包含完整的、可直接复制粘贴运行的代码。
- **例外：可以删除各文档末尾的"提交代码"小节**（即 `git add . && git commit -m "..."` 形式的 bash 代码块及其标题）。这是纯粹的仪式性操作说明，不影响教程的完整性。
- 只允许修改代码块**之外**的说明文字（引导语、解读、表格、过渡句等）。
- 每个代码块前必须有引导文字，说明"现在我们要做什么"
- 代码块后应有"翻译成人话"式的解读（用表格或段落逐段拆解）
- **文档中不使用任何表情符号（emoji）。** 包括但不限于：💡、⚠️、✅、❌、🎯、📖、🚀 等。保持纯文本形式。代码对比用文字标注（如"正确："、"错误："），不用 ✅/❌。

### 表格使用

- 概念对比用表格（不用段落堆砌）
- 配置项说明用表格
- 步骤流程用带编号的表格

### 统一术语

| 术语 | 统一写法 |
|------|---------|
| 测试方法命名 | `user_can_view_questions()` 下划线风格 |
| 测试结构 | Given-When-Then（不用 AAA） |
| TDD 循环 | "红 - 绿 - 重构" |
| MockMvc | MockMvc（驼峰，非 mockmvc） |
| Testcontainers | Testcontainers（首字母大写） |

### 一句话检查清单

写完一节后自问：**读者读完这一节，能立刻动手做什么？** 如果答案是"什么也做不了，只是知道了一些概念"，这一节需要重写。

---

## 文档编辑约定

### 文件命名

- 使用中文数字序号：`1.序言.md`, `2.如何开始？.md`
- 文件名不需要与目录序号重复编号

### 章节结构模式

每个章节遵循统一的节奏：
```
编写测试 → 运行测试（失败） → 编写代码 → 运行测试（通过） → 重构 → 小结
```

### 内部链接

- 使用相对路径引用同项目的其他文档
- 被引用的文档必须已存在（写文档前先确认目标文件存在）

---

## Git 约定

- 当前分支：`main`
- 提交信息用中文，描述改了什么
- 不要 force push 到 main
- 不要修改 git config
- 提交前确认用户意图，不主动提交

---

## 参考资源

- 《凤凰架构》开源文档：https://icyfenix.cn
- 配套工程：https://github.com/fenixsoft/awesome-fenix
- 本项目源码：https://github.com/qianhuihuiji/build-zhihu-with-springboot-and-tdd
- 本文档源码：https://github.com/qianhuihuiji/build-zhihu-with-springboot-and-tdd-manual
