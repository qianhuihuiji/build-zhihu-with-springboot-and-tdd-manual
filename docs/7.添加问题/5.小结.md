# 小结

恭喜你完成了第七章的学习！现在你已经掌握了创建问题功能的完整 TDD 开发流程。让我们来总结一下本章的内容。

---

## 一、本章学习要点

### 1.1 创建问题

我们学习了如何实现创建问题功能：

| 知识点 | 说明 |
|--------|------|
| 权限控制 | 未登录用户返回 401 |
| 分类功能 | 支持问题分类，添加 category 表 |
| 数据库迁移 | 使用 Flyway 管理表结构变更 |
| TDD 流程 | 测试 → 实现 → 测试通过 |

### 1.2 表单验证

我们学习了如何添加表单验证：

| 知识点 | 说明 |
|--------|------|
| 内置注解 | `@NotBlank`、`@NotNull` |
| 自定义 Validator | 实现 `ConstraintValidator` 接口 |
| 自定义注解 | 使用 `@Constraint` 元注解 |
| 验证规则 | title、content、categoryId 必填 |

### 1.3 发布问题

我们学习了如何实现发布问题功能：

| 知识点 | 说明 |
|--------|------|
| 发布功能 | 设置 `published_at` 时间戳 |
| 权限控制 | 使用 `@PreAuthorize` 和 Policy |
| 扩展 Mapper | 使用 `QuestionMapperExt` 添加自定义 SQL |
| 测试覆盖 | 未登录、非作者、作者三种场景 |

### 1.4 邀请某人

我们学习了如何实现@用户功能：

| 知识点 | 说明 |
|--------|------|
| 事件机制 | 使用 Spring ApplicationEvent |
| 事件发布者 | 使用 `ApplicationEventPublisher` |
| 事件监听器 | 使用 `@EventListener` 注解 |
| 正则表达式 | 使用正则提取@的用户名 |
| 通知表 | 创建 notification 表存储通知 |

---

## 二、功能完成清单

### 2.1 创建问题功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 未登录用户不能创建 | ✅ 完成 | 返回 401 未授权 |
| 登录用户可以创建 | ✅ 完成 | 创建问题记录 |
| 问题包含分类 | ✅ 完成 | 支持问题分类功能 |

### 2.2 表单验证功能

| 功能 | 状态 | 说明 |
|------|------|------|
| title 必填验证 | ✅ 完成 | 使用 `@NotBlank` |
| content 必填验证 | ✅ 完成 | 使用 `@NotBlank` |
| category_id 必填验证 | ✅ 完成 | 使用 `@NotNull` |
| category_id 有效验证 | ✅ 完成 | 使用自定义 `Validator` |

### 2.3 发布问题功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 未登录用户不能发布 | ✅ 完成 | 返回 401 未授权 |
| 登录用户可以发布 | ✅ 完成 | 设置 published_at |
| 只有作者可以发布 | ✅ 完成 | 使用 QuestionPolicy 验证 |

### 2.4 邀请某人功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 创建通知表 | ✅ 完成 | 存储通知信息 |
| 解析@用户 | ✅ 完成 | 从内容中提取用户名 |
| 发送通知 | ✅ 完成 | 被@的用户收到通知 |
| 支持多人@ | ✅ 完成 | 支持@多个用户 |

---

## 三、测试总结

### 3.1 集成测试

| 测试类 | 测试内容 |
|--------|---------|
| `CreateQuestionsTest` | 创建问题、表单验证 |
| `PublishQuestionsTest` | 发布问题、权限控制 |
| `InviteUserTest` | 邀请用户、通知功能 |

### 3.2 测试覆盖情况

| 功能 | 集成测试 | 单元测试 |
|------|---------|---------|
| 创建问题 | ✅ | - |
| 表单验证 | ✅ | - |
| 发布问题 | ✅ | - |
| 权限控制 | ✅ | ✅ (Policy 测试) |
| 邀请用户 | ✅ | - |

---

## 四、技术要点

### 4.1 TDD 流程

```
┌─────────────────────────────────────────────────────────┐
│                  第七章 TDD 流程                         │
│                                                         │
│  1. 编写集成测试 ──────→ 2. 运行测试（失败）           │
│                               ↓                         │
│  4. 编写单元测试 ←──────── 3. 创建数据模型              │
│       ↓                       ↓                         │
│  7. 运行测试（通过） ←──── 5. 实现 Service 层           │
│       ↓                                                 │
│  8. 完善 Controller ──────→ 9. 运行全部测试            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.2 表单验证方式对比

| 方式 | 说明 | 使用场景 |
|------|------|---------|
| `@NotBlank` | 验证字符串非空 | 必填字段 |
| `@NotNull` | 验证对象非 null | 必填对象 |
| 自定义 Validator | 复杂业务验证 | 验证分类存在等 |
| Policy | 权限验证 | 验证用户权限 |

### 4.3 Spring 事件机制

```
┌─────────────────────────────────────────────────────────┐
│              Spring 事件机制流程                         │
│                                                         │
│  事件源 ──────→ 发布事件 ──────→ 事件总线              │
│                                         ↓               │
│                              ┌─────────────────┐       │
│                              │   事件监听器     │       │
│                              │  (可能有多个)   │       │
│                              └─────────────────┘       │
│                                         ↓               │
│                              执行具体业务逻辑           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.4 通知数据结构

```
┌─────────────────────────────────────────────────────────┐
│                   notification 表                        │
├──────────┬──────────┬────────────┬──────────┬───────────┤
│ 字段     │ 类型     │ 说明       │ 示例     │ 必填     │
├──────────┼──────────┼────────────┼──────────┼───────────┤
│ id       │ int      │ 主键       │ 1        │ 是       │
│ type     │ varchar  │ 通知类型   │ 事件类名  │ 是       │
│ notifiable_id │ int │ 接收者 ID   │ 1        │ 是       │
│ notifiable_type │ varchar │ 接收者类型 │ User    │ 是       │
│ data     │ text     │ 通知内容   │ JSON     │ 是       │
│ read_at  │ timestamp│ 已读时间   │ null     │ 否       │
│ created_at | timestamp | 创建时间  │ 2026-... │ 是       │
│ updated_at | timestamp | 更新时间  │ 2026-... │ 是       │
└──────────┴──────────┴────────────┴──────────┴───────────┘
```

---

## 五、代码结构

### 5.1 目录结构

```
src
├── main
│   ├── java
│   │   └── com.nofirst.spring.tdd.zhihu.startup
│   │       ├── controller
│   │       │   ├── QuestionController.java
│   │       │   └── PublishedQuestionController.java
│   │       ├── service
│   │       │   └── impl/QuestionServiceImpl.java
│   │       ├── mapper
│   │       │   ├── QuestionMapper.java
│   │       │   └── QuestionMapperExt.java
│   │       ├── model
│   │       │   └── dto
│   │       │       └── QuestionDto.java
│   │       ├── policy
│   │       │   └── QuestionPolicy.java
│   │       ├── validator
│   │       │   ├── ValidCategory.java
│   │       │   └── ValidCategoryValidator.java
│   │       ├── event
│   │       │   └── PublishQuestionEvent.java
│   │       ├── publisher
│   │       │   └── CustomEventPublisher.java
│   │       ├── listener
│   │       │   └── PublishQuestionEventListener.java
│   │       └── util
│   │           └── InviteUserUtil.java
│   └── resources
│       └── db/migration
│           ├── V2026030301__create_table_category.sql
│           ├── V2026030302__alter_table_question.sql
│           ├── V2026030303__init_category_data.sql
│           └── V2026030304__create_table_notification.sql
└── test
    └── java
        └── com.nofirst.spring.tdd.zhihu.startup
            ├── integration
            │   └── questions/
            │       ├── CreateQuestionsTest.java
            │       ├── PublishQuestionsTest.java
            │       └── InviteUserTest.java
            └── factory
                └── QuestionFactory.java
```

### 5.2 核心代码

#### 创建问题

```java
@PostMapping("/questions")
public CommonResult<String> store(@RequestBody @Validated @ValidCategory QuestionDto dto, 
                                  @AuthenticationPrincipal AccountUser accountUser) {
    questionService.store(dto, accountUser);
    return CommonResult.success("ok");
}
```

#### 发布问题

```java
@PostMapping("/questions/{questionId}/published-questions")
@PreAuthorize("@questionPolicy.isQuestionOwner(#questionId, #accountUser)")
public CommonResult<String> store(@PathVariable Integer questionId, 
                                  @AuthenticationPrincipal AccountUser accountUser) {
    questionService.publish(questionId);
    return CommonResult.success("ok");
}
```

#### 事件发布

```java
@Override
public void publish(Integer questionId) {
    questionMapperExt.publish(questionId, new Date());

    Question question = questionMapper.selectByPrimaryKey(questionId);
    customEventPublisher.firePublishQuestionEvent(question);
}
```

#### 事件监听

```java
@EventListener
public void notifyInvitedUsers(PublishQuestionEvent event) {
    Question question = event.getQuestion();
    String data = getData(question);
    List<String> invitedUsers = InviteUserUtil.extractInvitedUser(question.getContent());
    // 发送通知给被@的用户
}
```

---

## 六、下一步

完成本章学习后，请继续后续章节：

| 章节 | 内容 | 说明 |
|------|------|------|
| 第八章 | 问题互动 | 筛选、关注、推荐、举报 |
| 第九章 | 评论相关 | 评论功能测试 |
| 第十章 | 用户相关 | 用户动态、消息 |

---

## 七、同步代码

记得将代码提交到版本控制：

```bash
$ git add -A
$ git commit -m "ch07: 完成添加问题功能"
$ git push origin master
```

---

## 八、学习检查清单

完成本章后，你应该能够：

- [ ] 编写集成测试验证创建问题功能
- [ ] 使用 `@NotBlank` 和 `@NotNull` 进行表单验证
- [ ] 创建自定义 Validator 进行业务验证
- [ ] 使用 `@PreAuthorize` 进行权限控制
- [ ] 使用 Spring 事件机制解耦业务逻辑
- [ ] 创建事件发布者和监听器
- [ ] 使用正则表达式提取@用户名
- [ ] 使用 Flyway 管理数据库迁移
- [ ] 遵循 TDD 流程开发功能
- [ ] 使用扩展 Mapper 添加自定义 SQL

如果你能完成以上所有项目，恭喜你！你已经掌握了创建问题功能的完整开发流程！🎉

---

## 附录：关键代码片段

### 自定义 Validator

```java
@Component
@AllArgsConstructor
public class ValidCategoryValidator implements ConstraintValidator<ValidCategory, QuestionDto> {

    private CategoryMapper categoryMapper;

    @Override
    public boolean isValid(QuestionDto value, ConstraintValidatorContext context) {
        Category category = categoryMapper.selectByPrimaryKey(value.getCategoryId());

        if (Objects.isNull(category)) {
            context.disableDefaultConstraintViolation();
            context.buildConstraintViolationWithTemplate("问题分类不存在")
                    .addConstraintViolation();
            return false;
        }

        return true;
    }
}
```

### 自定义注解

```java
@Constraint(validatedBy = {ValidCategoryValidator.class})
@Target({METHOD, FIELD, ANNOTATION_TYPE, CONSTRUCTOR, PARAMETER})
@Retention(RUNTIME)
public @interface ValidCategory {
    String message() default "问题分类不存在";
    Class<?>[] groups() default {};
    Class<? extends Payload>[] payload() default {};
}
```

### 正则提取@用户

```java
public class InviteUserUtil {

    public static List<String> extractInvitedUser(String content) {
        List<String> invitedUser = new ArrayList<>();
        Pattern p = Pattern.compile("(?<=@)\\S+");
        Matcher m = p.matcher(content);
        while (m.find()) {
            String username = m.group();
            invitedUser.add(username);
        }
        return invitedUser;
    }
}
```

### 通知数据格式

```java
private String getData(Question question) {
    Map<String, String> data = new HashMap<>(2);
    data.put("title", question.getTitle());
    data.put("content", question.getContent());
    try {
        return objectMapper.writeValueAsString(data);
    } catch (JsonProcessingException e) {
        throw new RuntimeException(e);
    }
}
```
