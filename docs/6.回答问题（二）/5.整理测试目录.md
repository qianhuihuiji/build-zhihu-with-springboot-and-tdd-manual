为了更好的组织测试，我们将已有的测试文件调整下位置：

1. 在 *integration* 下新建 *answers* 和 *questions* 文件夹;
2. 将 *PostAnswersTest.java*和*BestAnswerTest.java* 移到 *answers* 目录中;
3. 将 *ViewQuestionsTest.java* 移到 *questions* 目录中;
4. 调整文件位置后，请相应修改以上文件的命名空间；
5. 然后我们要运行一下全部测试 ，确保功能没有被破坏。


以上代码改动较大，我们来提交一次代码：

```
$ git add .
$ git commit -m 'change test folders'
```