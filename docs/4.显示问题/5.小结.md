# 小结

恭喜你完成了第四章的学习！现在你已经掌握了显示问题功能的完整 TDD 开发流程。让我们来总结一下本章的内容。

---

## 本章学习要点

### 创建测试

我们学习了如何创建集成测试：

| 知识点 | 说明 |
|--------|------|
| 测试思路 | 使用 AAA 模式（Given-When-Then）组织测试 |
| 测试数据 | 使用 `QuestionFactory` 创建测试数据 |
| 数据清理 | 使用 `@BeforeEach` 清理测试数据 |
| HTTP 请求 | 使用 `MockMvc` 发送 `GET /questions/{id}` 请求 |
| 响应验证 | 使用 `jsonPath` 和 `assertThat` 验证响应 |

### 数据模型和工厂

我们学习了如何定义数据模型和工厂类：

| 知识点 | 说明 |
|--------|------|
| Flyway 迁移 | 使用 SQL 脚本创建 `question` 表 |
| MyBatis Generator | 自动生成 Mapper 和实体类 |
| 工厂模式 | 使用 `QuestionFactory` 简化测试数据创建 |
| VO 对象 | 使用 `QuestionVo` 返回给前端 |

### 让测试通过

我们学习了如何实现业务逻辑：

| 知识点 | 说明 |
|--------|------|
| 路径变量 | 使用 `@PathVariable` 获取 URL 参数 |
| Controller | 调用 Service 层处理业务 |
| Service 层 | 查询数据库并转换为 VO |
| 依赖注入 | 使用 `@AllArgsConstructor` 自动注入 |
| TDD 原则 | 小步前进，每次只做一个小改动 |

### 集成测试与单元测试

我们学习了两种测试类型的区别：

| 测试类型 | 特点 | 用途 |
|---------|------|------|
| 集成测试 | 启动完整 Spring 环境 | 验证端到端流程 |
| 单元测试 | 使用 Mock 隔离依赖 | 验证单一组件逻辑 |

---

## TDD 开发流程回顾

我们一起经历了一个功能完整的 `TDD` 开发流程：

```
┌─────────────────────────────────────────────────────────┐
│                  完整的 TDD 开发流程                     │
│                                                         │
│  1. 编写测试代码 ──────→ 2. 运行测试                    │
│                               ↓                         │
│  6. 再运行测试 ←──────── 5. 再修正错误                  │
│       ↑                       ↓                         │
│       │                   4. 再编写代码                 │
│       │                       ↓                         │
│       └──────────────── 3. 然后得到错误                 │
│                                                         │
│              最终测试通过 ✓                             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 详细步骤

| 步骤 | 动作 | 结果 |
|------|------|
| 1️⃣ | 编写测试代码 | `user_can_view_a_single_question()` |
| 2️⃣ | 运行测试 | ❌ 类不存在（Question、QuestionMapper 等） |
| 3️⃣ | 得到错误 | 创建数据表和工厂类 |
| 4️⃣ | 编写代码 | Flyway 迁移、Generator 生成代码 |
| 5️⃣ | 修正错误 | 运行测试 |
| 6️⃣ | 得到错误 | ❌ 404 路由不存在 |
| 7️⃣ | 编写代码 | 添加 `show()` 方法 |
| 8️⃣ | 修正错误 | 运行测试 |
| 9️⃣ | 得到错误 | ❌ 返回空对象 |
| 🔟 | 编写代码 | 实现 Service 业务逻辑 |
| 1️⃣1️⃣ | 运行测试 | ✅ 测试通过 |

---

## 核心概念总结

### TDD 理念

> 可能你会很不习惯这种方式，认为先写测试是在无中生有。确实，我们是在无中生有。

我们在思考如何写测试代码时，实际上就是我们对功能的构思跟对业务代码的设计，只是这个构思与设计现在还仅仅存在于我们的设想当中，于是我们开始从无到有地开始写代码。

### 测试的价值

> 当我们的测试通过，这就意味着我们的业务代码开发完成；之后我们对业务代码进行修改时，最开始编写的测试又为我们提供的充足的信心与保护：只要测试能通过，那么修改就是无害的。

这是不是很棒呢？

### TDD 开发流程

我们再次强调下 TDD 开发的流程：

> 首先编写一个测试，运行这个测试看着它失败。然后编写最少量的代码取得一些进展，再运行测试。如此不断重复，直到测试通过为止。最后，或许还要重构代码，测试能确保不破坏任何功能。

---

## 小步前进原则

### 为什么要小步前进？

| 原因 | 说明 |
|------|------|
| **快速反馈** | 及时发现问题，定位更容易 |
| **降低风险** | 每次改动小，出错概率低 |
| **保持信心** | 不断看到进步，增强信心 |
| **易于调试** | 问题范围小，容易定位 |

### 何谓取得进步？

> **测试从一个错误，进入到下一个错误，即为进步。**

当然，我们的最终目标，就是让我们的测试通过。

### 错误示例

```java
// ❌ 不好的做法：一次性写太多代码
@Test
void should_do_everything() {
    // 100 行测试代码...
}

// ✅ 好的做法：小步前进
@Test
void user_can_view_questions() { }

@Test
void user_can_view_a_single_question() { }

@Test
void user_can_create_question() { }
```

---

## 测试类型对比

### 集成测试特点

| 特点 | 说明 |
|------|------|
| 启动完整 Spring 环境 | 测试时间较长 |
| 使用真实数据库 | Testcontainers 创建容器 |
| 测试端到端流程 | 验证组件协作 |
| 适合测试 Controller | 验证 HTTP 请求处理 |

### 单元测试特点

| 特点 | 说明 |
|------|------|
| 不启动 Spring 容器 | 测试速度快 |
| 使用 Mock 对象 | 隔离外部依赖 |
| 测试单一组件 | 验证业务逻辑 |
| 适合测试 Service | 验证方法逻辑 |
