## 本节说明

上一节中，我们的创建并成功运行了第一个测试，这一节我们一起来让测试通过。

## 路由不存在

报错 404，很明显是「路由不存在」，这很正常，因为我们没有创建对应的controller。创建以下controller：

*com/nofirst/spring/tdd/zhihu/startup/controller/QuestionController.java*

```
package com.nofirst.spring.tdd.zhihu.startup.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class QuestionController {

    /**
     * Index common result.
     *
     */
    @GetMapping("/questions")
    public void index() {
        // do nothing here
    }
}

```

保存后再次运行测试，即可看到测试通过：

![](../../images/episode3_2026-02-08_12-34-26.png)

## 总结

目前为止，我们编写了第一个测试，且成功让测试通过。接下来我们将补一下一些测试相关小知识。

## 提交代码

让我们将本次更改纳入版本控制中：

```
$ git add -A
$ git commit -m "test view questions"
```
