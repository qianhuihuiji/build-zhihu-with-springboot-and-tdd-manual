## 两种测试命名方法

通常而言，编写一个测试一般有两种方式：

1. 方法名以 `test` 开头，其后为驼峰风格，如`testUserCanViewQuestions()`;
2. 方法名为下划线风格；

**一般推荐使用第 2 种**，因其具有更佳的可读性，因此本教程中所有测试方法均为此风格。

## 提交代码

让我们将本次更改纳入版本控制中：

```
$ git add -A
$ git commit -m "rename tests"
```
