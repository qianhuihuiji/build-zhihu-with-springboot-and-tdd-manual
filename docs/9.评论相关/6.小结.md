# 小结

恭喜你完成了第九章的学习！现在你已经掌握了评论功能的完整 TDD 开发流程。让我们来总结一下本章的内容。

---

## 一、本章学习要点

### 1.1 评论某个问题

我们学习了如何实现评论问题功能：

| 知识点 | 说明 |
|--------|------|
| 多态关联 | 使用 `commented_id` + `commented_type` 关联不同对象 |
| 权限控制 | 使用 `@PreAuthorize` 和 Policy 控制评论权限 |
| 表单验证 | 使用 `@Validated` 和 `@NotBlank` 验证必填项 |

### 1.2 评论某个回答

我们学习了如何实现评论回答功能：

| 知识点 | 说明 |
|--------|------|
| 代码复用 | 与评论问题逻辑相似 |
| 多态关联 | `commented_type` 为 `Answer` |
| 表单验证 | 提前使用 `@Validated` 是好习惯 |

### 1.3 赞同与反对

我们学习了如何实现评论投票功能：

| 知识点 | 说明 |
|--------|------|
| 代码复用 | 借鉴问题/答案的投票功能实现 |
| 投票表设计 | 使用同一张 vote 表，通过 resource_type 区分 |
| 防止重复 | 先查询是否存在，存在则不插入 |

### 1.4 查看评论

我们学习了如何实现查看评论功能：

| 知识点 | 说明 |
|--------|------|
| 分页查询 | 使用 PageHelper 实现评论分页 |
| 公开访问 | 评论列表接口不需要登录 |
| 多态查询 | 根据 `commented_type` 查询不同类型 |

### 1.5 艾特某人

我们学习了如何实现@用户通知功能：

| 知识点 | 说明 |
|--------|------|
| 事件机制 | 使用 Spring ApplicationEvent 解耦业务 |
| 事件监听 | 使用 `@EventListener` 监听事件 |
| 通知逻辑 | 提取@用户名并创建通知记录 |
| 代码复用 | 使用 InviteUserUtil 提取用户名 |

---

## 二、功能完成清单

### 2.1 评论功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 评论问题 | ✅ 完成 | 登录用户可评论已发布问题 |
| 评论回答 | ✅ 完成 | 登录用户可评论回答 |
| 权限控制 | ✅ 完成 | 未登录用户不能评论 |
| 表单验证 | ✅ 完成 | 评论内容必填 |

### 2.2 投票功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 赞同评论 | ✅ 完成 | 创建 vote_up 记录 |
| 反对评论 | ✅ 完成 | 创建 vote_down 记录 |
| 取消投票 | ✅ 完成 | 删除投票记录 |
| 防止重复 | ✅ 完成 | 同一用户只能投票一次 |

### 2.3 查看评论

| 功能 | 状态 | 说明 |
|------|------|------|
| 查看问题评论 | ✅ 完成 | 分页查询问题评论 |
| 查看回答评论 | ✅ 完成 | 分页查询回答评论 |
| 公开访问 | ✅ 完成 | 不需要登录 |

### 2.4@用户功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 解析@用户 | ✅ 完成 | 从评论内容提取用户名 |
| 发送通知 | ✅ 完成 | 被@用户收到通知 |
| 支持多人 | ✅ 完成 | 支持@多个用户 |

---

## 三、测试总结

### 3.1 集成测试

| 测试类 | 测试内容 |
|--------|---------|
| `QuestionCommentsTest` | 评论问题测试 |
| `AnswerCommentsTest` | 评论回答测试 |
| `UpVotesTest` | 评论赞同测试 |
| `DownVotesTest` | 评论反对测试 |
| `ViewCommentsTest` | 查看评论测试 |
| `MentionUsersTest` |@用户通知测试 |

### 3.2 测试覆盖情况

| 功能 | 集成测试 | 单元测试 |
|------|---------|---------|
| 评论问题 | ✅ | - |
| 评论回答 | ✅ | - |
| 评论投票 | ✅ | - |
| 查看评论 | ✅ | - |
|@用户通知 | ✅ | - |

---

## 四、技术要点

### 4.1 TDD 流程

```
┌─────────────────────────────────────────────────────────┐
│                  第九章 TDD 流程                         │
│                                                         │
│  1. 编写集成测试 ──────→ 2. 运行测试（失败）           │
│                               ↓                         │
│  4. 编写单元测试 ←──────── 3. 创建数据模型              │
│       ↓                       ↓                         │
│  7. 运行测试（通过） ←──── 5. 实现 Service 层           │
│       ↓                                                 │
│  8. 完善 Controller ──────→ 9. 运行全部测试            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.2 多态关联设计

```
┌─────────────────────────────────────────────────────────┐
│                  评论多态关联设计                        │
│                                                         │
│  comment 表                                             │
│  ├── commented_id (被评论对象 ID)                       │
│  └── commented_type (被评论对象类型)                    │
│                                                         │
│  commented_type = "Question" → 关联 question 表         │
│  commented_type = "Answer"   → 关联 answer 表           │
│  commented_type = "Comment"  → 关联 comment 表 (扩展)   │
│                                                         │
│  优势：                                                 │
│  1. 只需一张评论表                                     │
│  2. 易于扩展到更多类型                                 │
│  3. 查询简单高效                                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.3 投票表复用

```
┌─────────────────────────────────────────────────────────┐
│                   vote 表（复用）                        │
├──────────┬──────────┬────────────┬──────────┬───────────┤
│ user_id  │ voted_id │ resource   │ action   │ 说明      │
├──────────┼──────────┼────────────┼──────────┼───────────┤
│ 2        │ 1        │ Question   │ vote_up  │ 推荐问题  │
│ 2        │ 1        │ Answer     │ vote_up  │ 推荐回答  │
│ 2        │ 1        │ Comment    │ vote_up  │ 推荐评论  │
└──────────┴──────────┴────────────┴──────────┴───────────┘

说明：
- resource_type: Question、Answer 或 Comment
- action_type: vote_up 或 vote_down
- 同一张表支持三种资源的投票
```

### 4.4 Spring 事件机制

```
┌─────────────────────────────────────────────────────────┐
│              Spring 事件机制流程                         │
│                                                         │
│  事件源 ──────→ 发布事件 ──────→ 事件总线              │
│                                         ↓               │
│                              ┌─────────────────┐       │
│                              │   事件监听器     │       │
│                              │  (可能有多个)   │       │
│                              └─────────────────┘       │
│                                         ↓               │
│                              执行具体业务逻辑           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.5 评论功能接口汇总

| 功能 | 问题 | 回答 |
|------|------|------|
| 创建评论 | `POST /comments/questions/{id}` | `POST /comments/answers/{id}` |
| 查看评论 | `GET /comments/questions/{id}` | `GET /comments/answers/{id}` |
| 赞同评论 | `POST /comments/{id}/up-votes` | `POST /comments/{id}/up-votes` |
| 反对评论 | `POST /comments/{id}/down-votes` | `POST /comments/{id}/down-votes` |

---

## 五、代码结构

### 5.1 目录结构

```
src
├── main
│   ├── java
│   │   └── com.nofirst.spring.tdd.zhihu.startup
│   │       ├── controller
│   │       │   ├── QuestionCommentsController.java
│   │       │   ├── AnswerCommentsController.java
│   │       │   ├── CommentUpVoteController.java
│   │       │   └── CommentDownVoteController.java
│   │       ├── service
│   │       │   ├── QuestionCommentService.java
│   │       │   ├── AnswerCommentService.java
│   │       │   ├── CommentVoteUpService.java
│   │       │   ├── CommentVoteDownService.java
│   │       │   └── impl/
│   │       ├── model
│   │       │   ├── dto/CommentDto.java
│   │       │   └── vo/CommentVo.java
│   │       ├── policy
│   │       │   └── QuestionPolicy.java
│   │       ├── event
│   │       │   └── PostCommentEvent.java
│   │       ├── listener
│   │       │   └── PostCommentEventListener.java
│   │       └── factory
│   │           └── CommentFactory.java
│   └── resources
│       └── db/migration
│           └── V2026030701__create_table_comment.sql
└── test
    └── java
        └── com.nofirst.spring.tdd.zhihu.startup
            ├── integration
            │   └── comments/
            │       ├── QuestionCommentsTest.java
            │       ├── AnswerCommentsTest.java
            │       ├── UpVotesTest.java
            │       ├── DownVotesTest.java
            │       ├── ViewCommentsTest.java
            │       └── MentionUsersTest.java
            └── factory
                └── CommentFactory.java
```

---

## 六、下一步

完成本章学习后，请继续后续章节：

| 章节 | 内容 | 说明 |
|------|------|------|
| 第十章 | 用户相关 | 用户动态、活跃用户、未读消息 |

---

## 七、同步代码

记得将代码提交到版本控制：

```bash
$ git add -A
$ git commit -m "ch09: 完成评论相关功能"
$ git push origin master
```

---

## 八、学习检查清单

完成本章后，你应该能够：

- [ ] 使用多态关联设计评论表
- [ ] 使用 Policy 控制评论权限
- [ ] 使用 `@Validated` 进行表单验证
- [ ] 复用投票表支持多种资源
- [ ] 使用 PageHelper 实现评论分页
- [ ] 公开评论列表接口（不需要登录）
- [ ] 使用 Spring 事件机制解耦业务
- [ ] 使用正则表达式提取@用户名
- [ ] 遵循 TDD 流程开发功能

如果你能完成以上所有项目，恭喜你！你已经掌握了评论功能的完整开发流程！🎉

---

## 附录：关键代码片段

### 多态关联

```java
// 评论问题
Comment comment = new Comment();
comment.setCommentedId(questionId);
comment.setCommentedType(Question.class.getSimpleName());

// 评论回答
Comment comment = new Comment();
comment.setCommentedId(answerId);
comment.setCommentedType(Answer.class.getSimpleName());
```

### 权限控制

```java
@PostMapping("/comments/questions/{questionId}")
@PreAuthorize("@questionPolicy.canComment(#questionId)")
public CommonResult<String> store(...) {
    questionCommentService.comment(questionId, commentDto, accountUser);
    return CommonResult.success("ok");
}

// QuestionPolicy
public boolean canComment(Integer questionId) {
    Question question = questionMapper.selectByPrimaryKey(questionId);
    if (Objects.isNull(question)) {
        throw new QuestionNotExistedException();
    }
    if (Objects.isNull(question.getPublishedAt())) {
        throw new QuestionNotPublishedException();
    }
    return true;
}
```

### 事件机制

```java
// 发布事件
customEventPublisher.firePostCommentEvent(comment);

// 监听事件
@EventListener
public void notifyMentionedUsers(PostCommentEvent event) {
    Comment comment = event.getComment();
    List<String> mentionedUsers = InviteUserUtil.extractInvitedUser(comment.getContent());
    // 创建通知...
}
```

### 正则提取@用户

```java
public class InviteUserUtil {

    public static List<String> extractInvitedUser(String content) {
        List<String> invitedUser = new ArrayList<>();
        Pattern p = Pattern.compile("(?<=@)\\S+");
        Matcher m = p.matcher(content);
        while (m.find()) {
            String username = m.group();
            invitedUser.add(username);
        }
        return invitedUser;
    }
}
```
