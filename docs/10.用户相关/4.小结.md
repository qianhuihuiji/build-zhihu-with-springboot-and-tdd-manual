### 小结

最后一章收尾了三个用户相关功能，也顺便引入了一些新的基础设施：

- **Activity 表 + 事件监听**：用户动态的实现再次验证了事件机制的价值——不需要修改 `QuestionServiceImpl.publish()` 或 `AnswerServiceImpl.store()` 的代码，只需要在监听器中加一个新的监听方法。
- **Redis + Testcontainers**：测试中通过 Testcontainers 启动了真实的 Redis 容器，保证了缓存逻辑是可测的。定时任务的逻辑也可以直接调 `calculateAndCacheActiveUsers()` 来验证。
- **自动化未读清理**：查看通知即标记已读，不需要用户手动操作。实现是两个 SQL 操作（查询 + 更新）的事务性配合。

到这里，整个教程的所有功能模块都完成了。从第一个 `void index() {}` 到活跃用户排名、Kafka 异步翻译、Redis 缓存——一路上每个功能都是测试驱动的。回顾一下，你写的测试已经为你积累了一张密实的保护网。继续写下去的时候，这张网会越来越密。
