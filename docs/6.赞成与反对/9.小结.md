# 小结

恭喜你完成了第六章的学习！现在你已经掌握了投票功能的完整 TDD 开发流程。让我们来总结一下本章的内容。

---

## 一、本章学习要点

### 1.1 赞同功能

我们学习了如何实现赞同功能：

| 知识点 | 说明 |
|--------|------|
| 投票表设计 | 使用唯一索引防止重复投票 |
| 枚举类型 | 使用 `VoteActionType` 定义投票类型 |
| 权限控制 | 未登录用户返回 401 |
| TDD 流程 | 测试 → 实现 → 测试通过 |

### 1.2 取消赞同

我们学习了如何实现取消赞同功能：

| 知识点 | 说明 |
|--------|------|
| DELETE 接口 | 使用 `@DeleteMapping` 定义删除接口 |
| 条件删除 | 使用 `VoteExample` 指定删除条件 |
| 测试验证 | 验证删除前后数据数量变化 |

### 1.3 不能重复赞同

我们学习了如何防止重复投票：

| 知识点 | 说明 |
|--------|------|
| 唯一索引 | 数据库层面防止重复数据 |
| 应用层验证 | 代码层面检查是否存在记录 |
| 优雅处理 | 重复操作时不抛异常，直接忽略 |

### 1.4 获取赞同数量

我们学习了如何查询投票数据：

| 知识点 | 说明 |
|--------|------|
| 查询方法 | `isVotedUp()` 和 `upVotesCount()` |
| 测试策略 | 单元测试 + 集成测试双重覆盖 |
| Mock 使用 | 单元测试中使用 Mock 隔离依赖 |

### 1.5 反对功能

我们学习了如何实现反对功能：

| 知识点 | 说明 |
|--------|------|
| 代码复用 | 与赞同功能类似，使用不同的 `action_type` |
| 同一张表 | 使用同一张 `vote` 表 |
| 枚举区分 | 使用 `VoteActionType.VOTE_DOWN` 标识反对 |

### 1.6 取消反对

我们学习了如何实现取消反对功能：

| 知识点 | 说明 |
|--------|------|
| 代码复用 | 与取消赞同逻辑完全相同 |
| DELETE 接口 | 使用 `@DeleteMapping` 定义删除接口 |
| 条件删除 | 使用 `VoteExample` 指定删除条件 |

### 1.7 不能重复反对

我们学习了如何防止重复反对：

| 知识点 | 说明 |
|--------|------|
| 代码复用 | 与防止重复赞同逻辑完全相同 |
| 唯一索引 | 数据库层面防止重复数据 |
| 应用层验证 | 代码层面检查是否存在记录 |

### 1.8 获取反对数量

我们学习了如何查询反对数据：

| 知识点 | 说明 |
|--------|------|
| 查询方法 | `isVotedDown()` 和 `downVotesCount()` |
| 代码复用 | 与赞同查询逻辑完全相同 |
| 测试策略 | 单元测试 + 集成测试双重覆盖 |

---

## 二、功能完成清单

### 2.1 投票功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 创建赞同 | ✅ 完成 | 登录用户可以赞同回答 |
| 取消赞同 | ✅ 完成 | 用户可以取消自己的赞同 |
| 防止重复赞同 | ✅ 完成 | 同一用户只能赞同一次 |
| 查询赞同状态 | ✅ 完成 | 查询是否已赞同 |
| 查询赞同数量 | ✅ 完成 | 查询赞同总数 |

### 2.2 反对功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 创建反对 | ✅ 完成 | 登录用户可以反对回答 |
| 取消反对 | ✅ 完成 | 用户可以取消自己的反对 |
| 防止重复反对 | ✅ 完成 | 同一用户只能反对一次 |
| 查询反对状态 | ✅ 完成 | 查询是否已反对 |
| 查询反对数量 | ✅ 完成 | 查询反对总数 |

---

## 三、测试总结

### 3.1 集成测试

| 测试类 | 测试内容 |
|--------|---------|
| `UpVotesTest` | 赞同、取消赞同、防止重复赞同、查询赞同 |
| `DownVotesTest` | 反对、取消反对、防止重复反对、查询反对 |

### 3.2 单元测试

| 测试类 | 测试内容 |
|--------|---------|
| `AnswerServiceImplTest` | `isVotedUp()`, `upVotesCount()`, `isVotedDown()`, `downVotesCount()` |

### 3.3 测试覆盖情况

| 功能 | 集成测试 | 单元测试 |
|------|---------|---------|
| 创建赞同 | ✅ | - |
| 取消赞同 | ✅ | - |
| 防止重复赞同 | ✅ | - |
| 查询赞同状态 | ✅ | ✅ |
| 查询赞同数量 | ✅ | ✅ |
| 创建反对 | ✅ | - |
| 取消反对 | ✅ | - |
| 防止重复反对 | ✅ | - |
| 查询反对状态 | ✅ | ✅ |
| 查询反对数量 | ✅ | ✅ |

---

## 四、技术要点

### 4.1 TDD 流程

```
┌─────────────────────────────────────────────────────────┐
│                  第六章 TDD 流程                         │
│                                                         │
│  1. 编写集成测试 ──────→ 2. 运行测试（失败）           │
│                               ↓                         │
│  4. 编写单元测试 ←──────── 3. 创建数据模型              │
│       ↓                       ↓                         │
│  7. 运行测试（通过） ←──── 5. 实现 Service 层           │
│       ↓                                                 │
│  8. 完善 Controller ──────→ 9. 运行全部测试            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 4.2 投票数据结构

```
┌─────────────────────────────────────────────────────────┐
│                      vote 表                             │
├──────────┬──────────┬────────────┬──────────┬───────────┤
│ user_id  │ voted_id │ resource   │ action   │ 说明      │
├──────────┼──────────┼────────────┼──────────┼───────────┤
│ 2        │ 1        │ Answer     │ vote_up  │ John 赞同 │
│ 3        │ 1        │ Answer     │ vote_up  │ 用户 3 赞同│
│ 4        │ 1        │ Answer     │ vote_down│ 用户 4 反对│
│ 2        │ 1        │ Answer     │ vote_down│ John 反对 │
└──────────┴──────────┴────────────┴──────────┴───────────┘

唯一索引：(user_id, voted_id, resource_type, action_type)
说明：同一用户对同一资源的同一操作只能有一条记录
      但可以有赞同和反对两条不同类型的记录
```

### 4.3 RESTful 投票接口

```java
// 赞同
POST   /answers/{answerId}/up-votes    // 创建赞同
DELETE /answers/{answerId}/up-votes    // 取消赞同

// 反对
POST   /answers/{answerId}/down-votes  // 创建反对
DELETE /answers/{answerId}/down-votes  // 取消反对
```

### 4.4 投票查询方法

```java
// AnswerService 接口
public interface AnswerService {
    // 赞同相关
    Boolean isVotedUp(Integer answerId);      // 是否被赞同
    long upVotesCount(Integer answerId);      // 赞同数量
    
    // 反对相关
    Boolean isVotedDown(Integer answerId);    // 是否被反对
    long downVotesCount(Integer answerId);    // 反对数量
}
```

---

## 五、代码结构

### 5.1 目录结构

```
src
├── main
│   ├── java
│   │   └── com.nofirst.spring.tdd.zhihu.startup
│   │       ├── controller
│   │       │   ├── AnswerUpVoteController.java
│   │       │   └── AnswerDownVoteController.java
│   │       ├── service
│   │       │   ├── AnswerVoteUpService.java
│   │       │   ├── AnswerVoteUpServiceImpl.java
│   │       │   ├── AnswerVoteDownService.java
│   │       │   └── AnswerVoteDownServiceImpl.java
│   │       ├── model
│   │       │   └── enums
│   │       │       └── VoteActionType.java
│   │       └── mapper
│   │           └── VoteMapper.java
│   └── resources
│       └── db/migration
│           └── V20260227__create_table_vote.sql
└── test
    └── java
        └── com.nofirst.spring.tdd.zhihu.startup
            ├── integration
            │   └── answers/
            │       ├── UpVotesTest.java
            │       └── DownVotesTest.java
            └── unit
                └── service
                    └── AnswerServiceImplTest.java
```

### 5.2 核心代码

#### 创建赞同

```java
@PostMapping("/answers/{answerId}/up-votes")
public CommonResult<String> store(@PathVariable Integer answerId, 
                                  @AuthenticationPrincipal AccountUser accountUser) {
    answerVoteUpService.store(answerId, accountUser);
    return CommonResult.success("ok");
}
```

#### 取消赞同

```java
@DeleteMapping("/answers/{answerId}/up-votes")
public CommonResult<String> destroy(@PathVariable Integer answerId, 
                                    @AuthenticationPrincipal AccountUser accountUser) {
    answerVoteUpService.destroy(answerId, accountUser);
    return CommonResult.success("ok");
}
```

#### 防止重复投票

```java
@Override
public void store(Integer answerId, AccountUser accountUser) {
    VoteExample voteExample = new VoteExample();
    VoteExample.Criteria criteria = voteExample.createCriteria();
    criteria.andVotedIdEqualTo(answerId);
    criteria.andResourceTypeEqualTo(Answer.class.getSimpleName());
    criteria.andActionTypeEqualTo(VoteActionType.VOTE_UP.getCode());
    long count = voteMapper.countByExample(voteExample);
    if (count == 0) {
        // 创建投票记录
        Vote vote = new Vote();
        // ...
        voteMapper.insert(vote);
    }
}
```

---

## 六、下一步

完成本章学习后，请继续后续章节：

| 章节 | 内容 | 说明 |
|------|------|------|
| 第七章 | 添加问题 | 创建问题功能 |
| 第八章 | 问题互动 | 筛选、关注、推荐、举报 |
| 第九章 | 评论相关 | 评论功能测试 |
| 第十章 | 用户相关 | 用户动态、消息 |

---

## 七、同步代码

记得将代码提交到版本控制：

```bash
$ git add -A
$ git commit -m "ch06: 完成赞成与反对功能"
$ git push origin master
```

---

## 八、学习检查清单

完成本章后，你应该能够：

- [ ] 编写集成测试验证投票功能
- [ ] 编写单元测试验证查询方法
- [ ] 使用 Flyway 创建投票表
- [ ] 使用枚举定义投票类型
- [ ] 使用唯一索引防止重复数据
- [ ] 实现防止重复投票逻辑
- [ ] 使用 VoteExample 查询投票数据
- [ ] 遵循 TDD 流程开发功能
- [ ] 使用 RESTful 风格设计接口
- [ ] 使用 Mock 进行单元测试

如果你能完成以上所有项目，恭喜你！你已经掌握了投票功能的完整开发流程！🎉

---

## 附录：关键代码片段

### VoteActionType 枚举

```java
public enum VoteActionType {

    VOTE_UP("vote_up", "赞同"),
    VOTE_DOWN("vote_down", "反对");

    private final String code;
    private final String description;

    VoteActionType(String code, String description) {
        this.code = code;
        this.description = description;
    }

    public String getCode() {
        return code;
    }

    public String getDescription() {
        return description;
    }
}
```

### VoteExample 查询

```java
// 查询赞同
VoteExample voteExample = new VoteExample();
VoteExample.Criteria criteria = voteExample.createCriteria();
criteria.andVotedIdEqualTo(answerId);
criteria.andResourceTypeEqualTo(Answer.class.getSimpleName());
criteria.andActionTypeEqualTo(VoteActionType.VOTE_UP.getCode());
long count = voteMapper.countByExample(voteExample);

// 查询反对
VoteExample downVoteExample = new VoteExample();
VoteExample.Criteria downCriteria = downVoteExample.createCriteria();
downCriteria.andVotedIdEqualTo(answerId);
downCriteria.andResourceTypeEqualTo(Answer.class.getSimpleName());
downCriteria.andActionTypeEqualTo(VoteActionType.VOTE_DOWN.getCode());
long downCount = voteMapper.countByExample(downVoteExample);
```

### 净胜票数计算

```java
// 计算净胜票数（可用于回答排序）
long upVotes = answerService.upVotesCount(answerId);
long downVotes = answerService.downVotesCount(answerId);
long netVotes = upVotes - downVotes;

// 按净胜票数排序回答
List<Answer> answers = answers.stream()
    .sorted(Comparator.comparingLong(a -> 
        calculateNetVotes(a.getId())
    ).reversed())
    .collect(Collectors.toList());
```
