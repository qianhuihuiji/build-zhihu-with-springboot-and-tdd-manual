# 取消反对

本节我们开发「取消反对」的功能。

---

## 一、功能说明

### 1.1 取消反对功能

与取消赞同类似，用户在反对某个回答后，应该能够取消自己的反对操作。

### 1.2 开发目标

| 目标 | 说明 |
|------|------|
| 删除投票记录 | 取消反对即删除对应的 vote 记录 |
| 验证删除成功 | 数据库中不再存在该投票记录 |

---

## 二、新增测试

### 2.1 添加测试用例

我们从新增测试开始：

*src/test/java/com/nofirst/spring/tdd/zhihu/startup/integration/answers/DownVotesTest.java*

```java
@Test
@WithUserDetails(value = "John", userDetailsServiceBeanName = "customUserDetailsService")
void an_authenticated_user_can_cancel_vote_down() throws Exception {
    // given
    this.mockMvc.perform(post("/answers/1/down-votes"));
    VoteExample voteExample = new VoteExample();
    VoteExample.Criteria criteria = voteExample.createCriteria();
    criteria.andVotedIdEqualTo(1);
    criteria.andResourceTypeEqualTo(Answer.class.getSimpleName());
    criteria.andActionTypeEqualTo(VoteActionType.VOTE_DOWN.getCode());
    long voteCount = voteMapper.countByExample(voteExample);
    assertThat(voteCount).isEqualTo(1);
    // when
    this.mockMvc.perform(delete("/answers/1/down-votes"))
            .andDo(print())
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(ResultCode.SUCCESS.getCode()));

    // then
    long voteCountAfter = voteMapper.countByExample(voteExample);
    assertThat(voteCountAfter).isEqualTo(0);
}
```

### 2.2 测试说明

| 步骤 | 说明 |
|------|------|
| `given` | 先创建一个反对记录，验证存在 1 条记录 |
| `when` | 发送 DELETE 请求取消反对 |
| `then` | 验证数据库中不再有该投票记录 |

### 2.3 测试流程

```
┌─────────────────────────────────────────────────────────┐
│              取消反对测试流程                            │
│                                                         │
│  1. 创建反对 ──────→ 2. 验证存在 1 条记录               │
│         ↓                                               │
│  4. 验证删除成功 ←────── 3. 取消反对                    │
│     (0 条记录)                                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.4 运行测试

运行功能测试：

![测试失败 -404](../../images/episode7_2026-03-02_14-50-11.png)

---

## 三、添加路由

### 3.1 新增 DELETE 接口

路由错误，新增路由：

*src/main/java/com/nofirst/spring/tdd/zhihu/startup/controller/AnswerDownVoteController.java*

```java
@DeleteMapping("/answers/{answerId}/down-votes")
public CommonResult<String> destroy(@PathVariable Integer answerId, @AuthenticationPrincipal AccountUser accountUser) {
    return CommonResult.success("ok");
}
```

### 3.2 运行测试

再次运行测试：

![测试失败 - 逻辑未实现](../../images/episode7_2026-03-02_15-26-33.png)

---

## 四、实现 Service 层

### 4.1 添加 destroy 方法

补充完善逻辑：

*src/main/java/com/nofirst/spring/tdd/zhihu/startup/service/AnswerVoteDownService.java*

```java
public interface AnswerVoteDownService {

    void store(Integer answerId, AccountUser accountUser);

    void destroy(Integer answerId, AccountUser accountUser);
}
```

### 4.2 实现删除逻辑

*src/main/java/com/nofirst/spring/tdd/zhihu/startup/service/impl/AnswerVoteDownServiceImpl.java*

```java
@Override
public void destroy(Integer answerId, AccountUser accountUser) {
    VoteExample voteExample = new VoteExample();
    VoteExample.Criteria criteria = voteExample.createCriteria();
    criteria.andVotedIdEqualTo(answerId);
    criteria.andResourceTypeEqualTo(Answer.class.getSimpleName());
    criteria.andActionTypeEqualTo(VoteActionType.VOTE_DOWN.getCode());
    voteMapper.deleteByExample(voteExample);
}
```

### 4.3 完善 Controller

*src/main/java/com/nofirst/spring/tdd/zhihu/startup/controller/AnswerDownVoteController.java*

```java
@DeleteMapping("/answers/{answerId}/down-votes")
public CommonResult<String> destroy(@PathVariable Integer answerId, @AuthenticationPrincipal AccountUser accountUser) {
    answerVoteDownService.destroy(answerId, accountUser);
    return CommonResult.success("ok");
}
```

### 4.4 运行测试

再次运行测试，测试通过。

---

## 五、总结

### 5.1 本节学习要点

| 要点 | 说明 |
|------|------|
| 取消反对 | 使用 DELETE 方法删除投票记录 |
| 条件删除 | 使用 VoteExample 指定删除条件 |
| 代码复用 | 与取消赞同逻辑类似 |

### 5.2 完整接口

```java
@RestController
@AllArgsConstructor
public class AnswerDownVoteController {

    private final AnswerVoteDownService answerVoteDownService;

    // 创建反对
    @PostMapping("/answers/{answerId}/down-votes")
    public CommonResult<String> store(@PathVariable Integer answerId, 
                                      @AuthenticationPrincipal AccountUser accountUser) {
        answerVoteDownService.store(answerId, accountUser);
        return CommonResult.success("ok");
    }

    // 取消反对
    @DeleteMapping("/answers/{answerId}/down-votes")
    public CommonResult<String> destroy(@PathVariable Integer answerId, 
                                        @AuthenticationPrincipal AccountUser accountUser) {
        answerVoteDownService.destroy(answerId, accountUser);
        return CommonResult.success("ok");
    }
}
```

---

## 六、提交代码

最后，我们提交一下代码：

```bash
$ git add .
$ git commit -m 'cancel vote down'
```

---

## 七、下一步

取消反对功能已完成，请继续阅读：

1. [不能重复反对](./7.不能重复反对.md) - 防止重复反对
2. [获取反对数量](./8.获取反对数量.md) - 查询反对数据

---

## 附录：投票操作对比

| 操作 | 赞同 | 反对 |
|------|------|------|
| 创建 | `POST /answers/{id}/up-votes` | `POST /answers/{id}/down-votes` |
| 取消 | `DELETE /answers/{id}/up-votes` | `DELETE /answers/{id}/down-votes` |
| Service | `AnswerVoteUpService` | `AnswerVoteDownService` |
| action_type | `vote_up` | `vote_down` |
